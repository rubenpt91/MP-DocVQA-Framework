save_dir: save/
model_name: LongT5
# model_weights: google/long-t5-tglobal-base  # Best pretrained.
# model_weights: tryolabs/long-t5-tglobal-base-blogpost-cqa
model_weights: rubentito/longt5-tglobal-base-mpdocvqa
device: cuda
max_sequence_length: 4096

training_parameters:
  lr: 1e-4  # 2e-4
  batch_size: 10
  train_epochs: 10
  warmup_iterations: 1000
